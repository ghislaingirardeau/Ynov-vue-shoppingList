// CHARGE LE CSS
import './assets/main.css'

// FUNCTION DE VUE JS
import { createApp } from 'vue'

// CHARGE L'INSTANCE DE VUE (le composant root)
import App from './App.vue'

// MOUNT (construit) LE COMPOSANT DANS LA DIV avec ID "app" 
createApp(App).mount('#app')
